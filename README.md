# Synapse Example App
An example of how to build and deploy a synapse app

## Setup
```bash
# Make sure docker is installed
./build_docker.sh

# The above might take a while as it is building the image
```

## Building
```bash
./start_docker.sh

./build.sh -a --configure
```
